# Security

This is a local-first single-user Todo application.

It has no account system and stores data in a local JSON file. Do not use it for sensitive data without adding appropriate authentication, storage encryption, access controls, backups, and a retention policy.

Never commit `.env` or credentials.
